from module statistics load Floor, Mean
load module data


v = [].random((10, (500, 1000)))
print * Floor(5.235) * Mean(), v

if v.size() == 10:
	if v[0] % 2 == 0:
		v = v.sorted( reverse = True)
	elif v[0] >= 600:
		v.init()
	else:
		v = v.sorted( False )
	end:
else:
	v.init()
end:

m = 100

switch m:
case 100:
	m = m % 2
case 200:
	m = 0
default:
	m = -1
end:

N = 1
unless N:
	N = True 
else:
	N = False
end:

print * v, m, N
